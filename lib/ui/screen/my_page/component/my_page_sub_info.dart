import 'package:flutter/material.dart';

class MyPageSubInfo extends StatelessWidget {
  const MyPageSubInfo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
