import 'package:flutter/material.dart';

class ProfileSubInfo extends StatelessWidget {
  const ProfileSubInfo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
